$(document).ready(function(){
    $('.slick_role').slick({
        arrows:true,
        dots:true,
        adaptiveHeight:true,
        speed:1000,
        easing:'ease',
    });
});

$(document).ready(function(){
    $('.slider_row').slick({
        // arrows:true,
        // dots:true,
        // adaptiveHeight:true,
        // speed:1000,
        // easing:'ease',
    });
});